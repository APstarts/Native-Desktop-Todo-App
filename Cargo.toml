[package]
name = "slint-learning"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.17.1"
tokio={version= "1",features=["full"]}
sqlx = { version = "0.9", features = ["runtime-tokio", "sqlite", "migrate"] }
dotenv = "0.15.0"

[build-dependencies]
slint-build = "1.17.1"
