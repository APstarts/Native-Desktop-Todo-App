export component SettingsPage {
    VerticalLayout {
        padding: 20px;
        spacing: 10px;
        Text {
            text: "Settings";
            font-size: 24px;
        }

        Text {
            text: "Settings will go here";
        }
    }
}
